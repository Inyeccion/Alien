using System.Collections.Generic;
using System.Numerics;

public class MoveToNode : BTNode
{
    private Vector3 destination;

    public NodeState Tick()
    {
        if (AIContext.)
    }

    public MoveToNode()
    {
        nodeType = NodeType.moveToNode;

        children = new List<BTNode>();
    }
}
